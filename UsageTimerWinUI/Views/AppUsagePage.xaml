<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UsageTimerWinUI.Views.AppUsagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UsageTimerWinUI"
    xmlns:models="using:UsageTimerWinUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">

        <ListView x:Name="AppList" ItemsSource="{x:Bind AppListItems}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:AppUsageRecord">
                    <Grid ColumnDefinitions="40,*,100,120" Padding="6">

                        <Image Source="{x:Bind Icon}" Width="28" Height="28"/>

                        <TextBlock Grid.Column="1" Text="{x:Bind Name}" FontSize="16"/>

                        <TextBlock Grid.Column="2" Text="{x:Bind Minutes}" HorizontalAlignment="Right"/>

                        <TextBlock Grid.Column="3" Text="{x:Bind Formatted}" HorizontalAlignment="Right"/>

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


    </Grid>
</Page>

