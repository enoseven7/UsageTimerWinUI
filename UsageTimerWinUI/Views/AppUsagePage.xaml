<Page
    x:Class="UsageTimerWinUI.Views.AppUsagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Padding="20" RowDefinitions="Auto,*,Auto" RowSpacing="12">

        <!-- SEARCHABLE DROPDOWN FOR ADDING APPS -->
        <ComboBox x:Name="ProcessDropdown"
                  PlaceholderText="Search running apps..."
                  Width="300"
                  IsEditable="True"
                  TextSubmitted="ProcessDropdown_TextSubmitted"
                  />

        <!-- LIST OF TRACKED APPS -->
        <controls:DataGrid x:Name="AppList"
                           Grid.Row="1"
                           AutoGenerateColumns="False"
                           ItemsSource="{x:Bind AppListItems, Mode=OneWay}"
                           IsReadOnly="True"
                           RowHeight="40">

            <controls:DataGrid.Columns>
                <controls:DataGridTemplateColumn Header="Icon" Width="50">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Width="32" Height="32" Source="{Binding Icon}"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
                <controls:DataGridTextColumn Header="App" Binding="{Binding Name}" Width="*" />
                <controls:DataGridTextColumn Header="Minutes" Binding="{Binding Minutes}" Width="120" />
                <controls:DataGridTextColumn Header="Time" Binding="{Binding Formatted}" Width="160" />

            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <Button Grid.Row="2"
                Content="Remove selected"
                HorizontalAlignment="Left"
                Click="RemoveButton_Click"/>
    </Grid>
</Page>
